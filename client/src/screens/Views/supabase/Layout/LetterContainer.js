import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LetterContainer = () => {
  return (
    <View>
      <Text>LetterContainer</Text>
    </View>
  );
};

export default LetterContainer;

const styles = StyleSheet.create({});
